# Backend notes

This folder tracks API changes needed in existing private backend services before the native app can be useful.

Public-safety rule: document API shape only. Do not commit private deployment paths, service source code, database files, tokens, or real domains.
